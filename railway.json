{
  "build": {
    "commands": [
      "cd frontend && npm ci && npm run build"
    ]
  },
  "deploy": {
    "startCommand": "gunicorn run:app -b 0.0.0.0:$PORT -w 4",
    "restartPolicyType": "ON_FAILURE"
  }
}